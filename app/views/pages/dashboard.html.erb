
<% content_for :meta_description, "Clean your carbon footprint by donating to associations" %>
<% content_for :meta_image, "rails-photo.jpg" %>
<div id="trip-form">
    <%= render 'trips/new', trip: @trip %>
</div>

<div class="wrapper">
  <div class="container">
    <div id="mytrips">
      <%= render 'trips/index' %>
    </div>
    <div class="row" id="projects">
      <%= render 'projects/index' %>
    </div>
  </div>
</div>
